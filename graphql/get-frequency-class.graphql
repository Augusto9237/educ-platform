query GetFrequenciesClass($code: String = "") {
  frequencies(where: {turma: {code: $code}}) {
    createdAt
    id
    subscribes {
      ... on Presence {
        id
        prensente
        subscriber {
          name
          id
        }
      }
    }
  }
}

